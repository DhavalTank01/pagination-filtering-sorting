import React from "react";

const Navbar = ({ count }) => {
  return (
    <div className="">
      <p className="text-center  text-uppercase  h2">all movies list {count}</p>
    </div>
  );
};

export default Navbar;
